# 05 · 反思沉淀与技能落地 — 优化设计文档（SDD 增量）

> 评审轮次：Round 3（架构师审查 → 优化设计 → 任务拆解 → 实施 → QA）
> 基线：`32 个测试文件 / 476 测试全绿`（vitest run，2025-08-28）
> 本文是 `doc/02-软件设计文档-SDD.md` 的增量修订，不替代原文。
> **修订记录**：Round 3 实施中断后由架构师复核（2026-08-28），新增 §1.5 工作树现状评估
> （逐文件裁定 + 验证记录 + 设计-实现偏差登记），§5 任务拆解移交 `doc/06-任务文档-Round3.md`。

---

## 1. 审查结论（事实 + 证据）

### F1 ·【P0 · 断链】执行器知识沉淀（反思）只有"要求"，没有"实现"

- prompt 已要求执行器输出 `WEAVE_KNOWLEDGE_START/END` 标记
  （`delegation-service.ts:843-847`），测试也断言了模板存在
  （`delegation-service.test.ts:212-213`）。
- SDD 2.5.1/2.5.8 承诺"解析执行器输出中的 WEAVE_KNOWLEDGE 标记并沉淀 candidate"，
  但全仓库 `src/` 中**不存在任何解析与落库代码**（grep 仅命中 prompt 模板与文档）。
- 后果：SDD 4.3"角色学习闭环"最后一环（任务产出 → Pitfall/Pattern candidate → 再审核）
  实际断裂，执行器被要求输出标记但结果被静默丢弃。

### F2 ·【P0 · 断链】任务终态后无反思钩子；保温期机制未接线

- `WeaveScheduler.#afterTaskSettled`（`scheduler.ts:464`）只做：失败传播 SKIPPED →
  刷 DAG 状态 → 重泵。没有任何反思/学习入口。
- `FeedbackRouter.enterAwaitingFeedback` 注释"自动调用点：Orchestrator 委托完成路径"
  （`feedback-router.ts:98`），但生产路径从未调用（grep：仅测试调用）。
  COMPLETED 在调度器中直接终态，AWAITING_FEEDBACK/accept/revise 全链路不可达。
- 结论：**当前"任务执行后没有反思"**；反馈路由是有实现、无接线的半成品。

### F3 ·【P1】执行器执行中无法按需检索知识

- prompt 明示"P0 阶段无知识检索 CLI"（`delegation-service.ts:841`）。
- 注入是委托前一次性 `slim:true`（首段/≤200 字符）；MCP `dsh-mcp-knowledge`
  （`mcp/knowledge-mcp.ts`）只服务外部 ACP provider，DSH 内生执行器用不上。

### F4 ·【P1】知识"图谱"与注入脱节

- `web/knowledge-graph.ts` 仅为 [[双链]] UI 预览（不参与注入）；注入是
  freshness_first 权重平铺列表（`knowledge-engine.ts:50-58`）。
- 注入条目 `formatKnowledgeSection` 不输出知识 id（`delegation-service.ts:226-243`），
  执行器无法引用/回查某条知识。

### F5 ·【P1】沉淀协议与存储模型不匹配

- 模板 `{"type":"pitfall",...}` 无 layer/scope 字段；`KnowledgeType` 支持
  doc/skill/guide/pitfall/pattern/other（`knowledge-model.ts:20`），但 SDD 未定义
  执行器输出的 layer 路由规则，也未定义"不同执行器"的归属标记。

### F6 ·【P2】role 层知识只读不写

- 注入权重表给 role 层 0.8（同项目），但唯一写入路径 `ImportPipeline` 产 doc 类
  project 层卡片；role 层实际永远为空 → 角色维度注入形同虚设。

### F7 ·【P2】`scopeMatch` 用 `path.includes(project_id)` 子串匹配（`knowledge-mcp.ts:33-40`）

- `project_id=app` 会误命中 `application`；P0 可接受，需在文档标注。

### F8 ·【P2】`knowledge-mcp.ts` 输出总字符 2500 硬编码

- 与团队级 `knowledge_injection` 限额体系脱节。

---

## 1.5 Round 3 工作树现状评估（架构师复核，2026-08-28）

实施中断后工作树遗留多批未提交改动。本节为逐项裁定与验证记录，是 `doc/06` 任务拆解的依据。

### 1.5.1 验证现状（复核时实测，未改代码）

| 命令 | 结果 | 明细 |
| --- | --- | --- |
| `pnpm vitest run` | ✅ **35 文件 / 495 测试全绿** | 较基线 +3 文件 +19 测试（reflection 8、reflection-service 7、rpc 回归 2、host-wiring 1、scheduler-reflection 4 等分布） |
| `pnpm typecheck` | ❌ **6 errors** | 全部为 `reflection.test.ts` TS2532（`noUncheckedIndexedAccess` 下 `blocks[0]` 可能为 undefined），行号 58/83/84/85/92/102 |
| `pnpm lint` | ❌ **74 problems（34E / 40W）** | **本轮新增仅 1E**：`reflection-service.ts:134` `no-control-regex`；其余 33E 全为存量（`.artifacts/` 调试脚本目录 31E `no-undef`；`src/client/index.ts:3976` `TeamMessagePanel` unused；`client-bundle.ui.test.tsx:24` `vi` unused——后两者经 `git show HEAD` 核实在 HEAD 上即存在） |

结论：反思链路功能层面已完成且全绿；剩余工作为**修复类**（typecheck 6 处 + lint 新增 1 处 + lint 存量治理）与 **QA 回归**，无新实施类开发。

### 1.5.2 工作树逐文件裁定

**裁定总原则**：改动按批次归属分为「反思链路（本文 R1–R5）」「宿主接线健壮化」「UX/prompt 增强」「工具与文档」四批；逐批核对实现与规格/实证后裁定。

#### 批次 A：反思沉淀链路（本文 §3 R1–R5）——全部保留

| 文件 | 改动 | 裁定 | 理由 |
| --- | --- | --- | --- |
| `reflection.ts`（新增 108 行） | R1 解析器 | **保留** | 逐条符合 §3.1：标记兼容 0..6 个 `#` 与 CRLF、多块、START 无 END / 非法 JSON / 缺 title|content 计 invalid 不抛错、type/tags/layer 规整规则一致 |
| `__tests__/reflection.test.ts`（新增） | R1 单测 | **保留 + 修复** | 8/8 绿但残留 6 处 TS2532（任务 T1）；测试语义本身正确 |
| `reflection-service.ts`（新增 137 行） | R2 沉淀路由 | **保留 + 小修** | 符合 §3.2：layer 路由/instance 降级记 error/溯源三元组 tags/status=candidate/审计失败不回滚/单块失败收集 errors。两处小瑕疵：①`:134` 文件名消毒正则含控制字符类触发 `no-control-regex`（lint 新增 error，任务 T2）；②内联消毒正则与规格"经 `safeKnowledgeFilename` 消毒"表述不符（见 1.5.3 偏差 D2，无害） |
| `__tests__/reflection-service.test.ts`（新增） | R2 单测 | **保留** | 7/7 绿，覆盖路由/降级/审计/容错 |
| `scheduler.ts`（+24 行） | R3 钩子 | **保留** | `onTaskSettledText` 可选注入、COMPLETED/FAILED 各一次、异常 `log.warn` 吞掉、`>0` 通知，与 §3.3 一致；不注入时行为不变 |
| `__tests__/scheduler-reflection.test.ts`（新增） | R3 单测 | **保留** | 4 用例：COMPLETED 参数与通知 / FAILED 触发 / 钩子抛错不阻断终态与 DAG 收敛 / 不注入行为不变 |
| `index.ts`（R4 部分） | 装配 | **保留** | `ReflectionService` + `onTaskSettledText` 接线符合 §3.4（audit 实例来源偏差见 1.5.3 D1） |
| `audit/audit-log.ts`（+3 行） | R4 审计 | **保留** | `knowledge.deposited` 三处注册（TYPES/联合类型/必填字段表）齐且位置正确 |
| `__tests__/audit-log.test.ts` | R4 审计单测 | **保留** | 新增正常路径（核心 9 类断言）与缺字段异常路径用例 |
| `delegation-service.ts`（`:237`） | R5 注入带 id | **保留** | `- [layer] title（id）：content` 与 §3.5 一致 |
| `__tests__/delegation-service.test.ts` | R5 断言 | **保留** | formatKnowledgeSection 与 prompt 快照断言已同步 |
| `knowledge-model.ts`（1 行） | R2 依赖 | **保留** | `LAYER_VISIBILITY` 由私有改导出，最小变更 |

#### 批次 B：宿主接线健壮化（早前独立修复，`.zcode/plans/plan-sess_a6767e50` 实证）——全部保留

| 文件 | 改动 | 裁定 | 理由 |
| --- | --- | --- | --- |
| `rpc.ts`（+22/-7） | webServer 改 `inject(['webServer'])` 延迟获取 | **保留** | 根因修复：直接读属性触发 cordis getter 抛错 `cannot get property "webServer" without inject`，曾连坐致 pre-step 钩子与 weave_* 工具全部未注册（宿主日志实证）；现 headless 自然降级 |
| `index.ts`（接线部分） | pre-step/RPC/host 三段独立 try-catch + pre-step 最先注册 | **保留** | 同类连坐 bug 第二次发生（上次 `agents`，commit 7d4b832），加固后单段失败只降级自身；与 R4 装配同文件无冲突 |
| `host-wiring.ts`（+16 行） | `resolveSessionId` 回调 + `weave_team_switch` 缺省解析 | **保留** | 消除假会话 id `'cli-session'` 绑定（显式 > exec 血统回溯 > CLI 兜底），复用 planner 同一真值链 |
| `__tests__/rpc.test.ts`（+78 行） | 回归用例 ×2 | **保留** | getter 抛错不炸接线 + HTTP fallback 信封，直接对应根因 |
| `__tests__/host-wiring.test.ts`（+28 行） | session 解析优先级用例 | **保留** | 四种输入组合覆盖 |

#### 批次 C：UX / prompt 增强（早前批次延续）——保留

| 文件 | 改动 | 裁定 | 理由 |
| --- | --- | --- | --- |
| `src/client/index.ts`（+164/-45） | 会话面板左右分栏可折叠、DAG 节点紧凑化、宿主输入框隐藏等 | **保留** | 与最近 5 个 UX 提交同系列；`client-bundle.ui.test.tsx` 20 用例绿。注意 `TeamMessagePanel` unused 为 HEAD 存量（任务 T3 顺手清理） |
| `delegation-service.ts`（DSH 提示部分） | prompt 注入「DSH Memory 提示」「执行纪律」两节 | **保留** | 与队长派发模板字段对齐；delegation-service.test.ts 快照断言已更新 |

#### 批次 D：工具与文档——保留

| 文件 | 裁定 | 理由 |
| --- | --- | --- |
| `scripts/captain-plan-tasks.mjs`（新增） | **保留** | 一次性 smoke 脚本（模拟队长派发，依赖 `dist/`），不参与构建/测试，无副作用 |
| `.zcode/plans/plan-sess_a6767e50-….md`（新增） | **保留** | 批次 B 的根因分析实证记录，含部署验证步骤，有长期参考价值 |

### 1.5.3 设计-实现偏差登记（均已核定可接受）

| # | 偏差 | 设计原文 | 实际实现 | 裁定 |
| --- | --- | --- | --- | --- |
| D1 | AuditLog 实例来源 | §3.4 伪代码 `audit: deps.auditLog!` | `index.ts:182` 自建 `new AuditLog({ dir: effectiveAuditDir })`（deps 内本无 auditLog 实例可复用） | **可接受**。`appendFile` 走 OS 级 `O_APPEND` 原子写，同进程双实例交错追加风险极低；后续若引入跨模块审计共享，统一提升为 deps 级单例（P2，不本轮） |
| D2 | filename 消毒方式 | §3.2「经 `safeKnowledgeFilename` 消毒」 | `reflection-service.ts:133-136` 内联正则预消毒；`KnowledgeStore.createCandidate`（`knowledge-model.ts:448`）内部本就调用 `safeKnowledgeFilename` 兜底 | **可接受**。实际是"服务侧预消毒 + store 侧兜底"双层消毒，行为等价且更防御；本文按实现修订表述（见 §3.2 注） |
| D3 | FAILED 分支钩子入参 text | §3.3「FAILED：result: text 已落库后」 | 实现传 FAILED 分支的 text 变量（fake 委托下即输出文本） | 一致，无偏差（列出备查：FAILED 时 text 为输出正文，diagnostic 不并入 text，Pitfall 沉淀以正文内 WEAVE_KNOWLEDGE 块为准） |

### 1.5.4 回退清单

**无任何文件需要回退。** 全部工作树改动经逐项核对后裁定保留；剩余动作见 `doc/06-任务文档-Round3.md`（T1–T4，均为修复/治理/回归类）。

---

## 2. 优化方案总览

| 编号 | 方案 | 优先级 | 状态（2026-08-28 复核） |
| --- | --- | --- | --- |
| R1 | `reflection.ts`：WEAVE_KNOWLEDGE 解析器（纯函数、容错、可计数） | P0 | ✅ 已实施，8/8 单测绿；测试文件残留 6 处 TS2532 待修（T1） |
| R2 | `ReflectionService`：沉淀路由（layer/scope/visibility/tags/审计） | P0 | ✅ 已实施，7/7 单测绿；1 处 lint error 待修（T2）；偏差 D1/D2 已核定 |
| R3 | Scheduler 终态反思钩子 `onTaskSettledText`（可选注入、异常不阻断） | P0 | ✅ 已实施，4/4 单测绿 |
| R4 | `index.ts` 装配：ReflectionService + 审计事件 `knowledge.deposited` | P0 | ✅ 已实施，audit-log 14/14 单测绿 |
| R5 | 注入条目带 id：`- [layer] title（id）：content` | P0 | ✅ 已实施，断言已同步 |
| R6 | 保温期（AWAITING_FEEDBACK）接线：终态视图扩展 + accept 重泵钩子 | P1 | 设计落案（§6.1），另轮实施 |
| R7 | 执行器按需检索（knowledge MCP 注入 provider / 执行器命令） | P1 | 设计落案（§6.2），另轮实施 |
| R8 | 图谱注入（Top-K 关联边摘要随知识条目下发） | P2 | 展望（§6.3） |

---

| 编号 | 方案 | 优先级 | 本轮 |
| --- | --- | --- | --- |
| R1 | `reflection.ts`：WEAVE_KNOWLEDGE 解析器（纯函数、容错、可计数） | P0 | ✅ |
| R2 | `ReflectionService`：沉淀路由（layer/scope/visibility/tags/审计） | P0 | ✅ |
| R3 | Scheduler 终态反思钩子 `onTaskSettledText`（可选注入、异常不阻断） | P0 | ✅ |
| R4 | `index.ts` 装配：ReflectionService + 审计事件 `knowledge.deposited` | P0 | ✅ |
| R5 | 注入条目带 id：`- [layer] title（id）：content` | P0 | ✅ |
| R6 | 保温期（AWAITING_FEEDBACK）接线：终态视图扩展 + accept 重泵钩子 | P1 | 设计落案，另轮实施 |
| R7 | 执行器按需检索（knowledge MCP 注入 provider / 执行器命令） | P1 | 设计落案，另轮实施 |
| R8 | 图谱注入（Top-K 关联边摘要随知识条目下发） | P2 | 展望 |

---

## 3. 详细设计（本轮实施 R1–R5）

### 3.1 R1 解析协议（`src/plugins/weave/reflection.ts`）

```text
<执行器自由输出>
### WEAVE_KNOWLEDGE_START
{"type": "pitfall", "title": "...", "content": "...", "tags": ["..."], "layer": "project"}
### WEAVE_KNOWLEDGE_END
```

- `extractKnowledgeBlocks(text): ReflectionParseResult`
  - 标记行兼容 0..6 个 `#` 前缀与 CRLF；支持一段输出中多个块；
  - 块内必须是合法 JSON 对象，否则记入 `invalid` 计数并跳过（不抛错）；
  - 字段规整：`type` ∉ 枚举 → `'other'`；`tags` 非字符串数组 → `[]`；
    `layer` ∉ {project,role,instance,shared} → 视为未指定；
  - `title`/`content` 空或缺失 → 记入 `invalid`；
  - `ReflectionParseResult = { blocks: WeaveKnowledgeBlock[]; invalid: number }`。

### 3.2 R2 沉淀路由（`src/plugins/weave/reflection-service.ts`）

`ReflectionService.depositFromOutput(input)`，输入：
`{ taskId, executor, roleId, projectId, version, outputText }`。

路由规则（缺省 project 层，可由块内 `layer` 覆盖）：

| layer | 目录（KnowledgeStore.resolveAgentDir） | visibility |
| --- | --- | --- |
| project | `_agent/projects/{projectId}/{version}/` | project_only |
| role | `_agent/roles/{roleId}/` | role_only |
| instance | 需要 instanceId；缺失 → 降级 project 并记 error | instance_only |
| shared | `_agent/shared/` | global |

- frontmatter：`type` 透传（枚举外 → other）；`status` 由 `createCandidate`
  强制 `candidate`（AC-KNOW-003：不直接 active）；`confidence=0.1`、
  `freshness_score=1.0` 由 store 强制默认值。
- tags 追加溯源三元组：`executor:{executor}`、`role:{roleId}`、
  `source:weave-reflection` —— **"不同执行器"的沉淀以 executor tag + role 层路由区分**。
- filename：`reflect-{taskId}-{序号}.md`，双层消毒——服务侧预消毒（剔除 `\/:*?"<>|` 与控制字符）+
  store 侧 `createCandidate` 内 `safeKnowledgeFilename` 兜底（偏差 D2 核定后的最终表述）。
- 每条成功沉淀写审计事件 `knowledge.deposited`
  `{ knowledge_id, task_id, executor, layer }`（追加式审计，失败不阻断状态）。
- 返回 `{ deposited: Array<{id,title,layer,path}>, invalid, errors }`；
  单块失败不抛错（降级收集 errors），全部失败/无块 → 空 deposited。
- 审计事件类型注册：`AUDIT_EVENT_TYPES` 增加 `'knowledge.deposited'`，
  必填字段表同步（`audit/audit-log.ts`）。

### 3.3 R3 调度器钩子（`scheduler.ts`）

```ts
WeaveSchedulerOptions 新增（全部可选，缺省行为不变）：
onTaskSettledText?: (params: {
  task: TaskRecord; role: RoleConfig; team: TeamConfigLike;
  text: string; status: 'COMPLETED' | 'FAILED';
}) => Promise<number | void>   // 返回沉淀条数（>0 时调度器发通知）
```

- 触发点：COMPLETED 分支（`result: text` 已落库后）与 FAILED 分支
  （`result: text` 已落库后），各一次；
- 钩子异常：`log.warn` 记录后吞掉（反思失败不阻断调度主链路，与知识注入降级一致）；
- 返回 `n > 0` → `notify(run, "[weave] 反思沉淀 n 条候选知识（待审核）")`。

### 3.4 R4 装配（`index.ts`）

```ts
const reflection = new ReflectionService({ knowledge: deps.knowledgeStore!, audit: deps.auditLog! })
const scheduler = new WeaveScheduler({
  ...,
  onTaskSettledText: async ({ task, role, text, status }) => {
    const result = await reflection.depositFromOutput({
      taskId: task.id, executor: role.executor, roleId: role.id,
      projectId: task.project_id, version: task.version, outputText: text,
    })
    return result.deposited.length
  },
})
```

FAILED 也沉淀：失败现场（含 diagnostic 文本）正是 Pitfall 的主要来源
（SDD 4.3"任务失败/高效产出 WEAVE_KNOWLEDGE 标记"）。

### 3.5 R5 注入条目带 id（`delegation-service.ts`）

`formatKnowledgeSection` 输出改为：

```text
- [layer] title（knowledge_id）：content
```

执行器从此可以引用知识 id（回查、supersede 建议均在协议上可行）。
同步更新受影响断言（`delegation-service.test.ts`）。

### 3.6 兼容性与降级

- 全部新能力为**可选注入**：不注入钩子时调度器行为与现状逐字节一致（476 测试不改动语义）；
- 反思解析器为纯函数，无 IO；沉淀失败不影响任务终态；
- 知识生命周期不变：反思只产 candidate，转正必须显式 approve。

---

## 4. 知识图谱 / 技能：现在怎么落地、怎么用（现状 + 本轮后）

### 4.1 注入（现在）

```text
任务委托前 DelegationService.executeTask
  → KnowledgeEngine.searchForInjection({projectId, version, roleId, limits, slim:true})
  → 仅 active 知识，按 weight×freshness 排序（项目1.0/跨版本0.9/实例0.85/角色0.8/全局0.6）
  → 限额（max_entries / max_chars_per_entry / max_total_chars，团队级唯一来源）
  → prompt「相关知识」段（本轮起每条带 id）
```

### 4.2 沉淀（本轮起可用）

```text
执行器输出 WEAVE_KNOWLEDGE 块（COMPLETED 或 FAILED）
  → Scheduler.onTaskSettledText
  → ReflectionService.depositFromOutput
  → layer 路由（project/role/shared；executor/role 溯源 tags）
  → knowledge.deposited 审计
  → 候选审核队列（CLI weave_knowledge_review / Web 知识页）→ approve → active → 下次注入生效
```

### 4.3 技能（skill）落地方式

- skill 是 `KnowledgeType` 枚举之一：执行器在 WEAVE_KNOWLEDGE 块中
  `"type":"skill"` 即沉淀为技能候选；审核转正后按 4.1 权重注入
  （项目技能 1.0 / 角色技能 0.8 / 全局技能 0.6）；
- 技能的"按执行器区分"：role 层技能只注入同 roleId 的执行器
  （`scoreKnowledge` roleSameProject=0.8 / roleCrossProject=0.4）；
- 图谱侧：skill 卡片参与 [[双链]] 图谱（`knowledge/graph` RPC），可在控制台知识页看关联。

### 4.4 使用方式（运维视角）

```bash
# 审核队列（默认 candidate）
pnpm vitest run src/plugins/weave/__tests__/cli-mcp.test.ts   # weave_knowledge_review/approve/reject 已有
# Web：控制台 → 知识页 → 候选审核；图谱预览 knowledge/graph
# MCP（外部 ACP）：dsh-mcp-knowledge → knowledge_search
```

---

## 5. 任务拆解

R1–R5 已实施（§2 状态列）。剩余收尾任务（修复 / lint 治理 / QA 回归）已拆解移交
**`doc/06-任务文档-Round3.md`**（T1–T4，按文件所有权切分、可并行、末位为 QA 全量回归
+ 文档-实现一致性检视），本文不再重复维护任务表。

## 6. P1 设计落案（本轮不实施）

### 6.1 R6 保温期接线（为什么现在不能直接开）

`AWAITING_FEEDBACK` 不在 `TERMINAL_STATUSES`（`scheduler.ts:72` 附近的
TERMINAL/SUCCESS 两张表），直接接线会导致 DAG 永不完成。正确做法：

1. `WeaveSchedulerOptions.feedbackWindowEnabled`（默认 false）；
2. 启用时：调度视图把 AWAITING_FEEDBACK 视为"挂起非终态"，DAG 完成判定改为
   `every(t => TERMINAL || t.status === 'AWAITING_FEEDBACK')`；
3. COMPLETED → `enterAwaitingFeedback`（复用 FeedbackRouter，保留其全部单测语义）；
4. accept（→CLOSED）/reject 后经 `deps.executionHooks` 新增 `onAccepted(taskId)`
   回调 `scheduler.enqueue(dagId)` 重泵下游；
5. e2e：会话面板验收按钮 → accept → 下游解锁。

### 6.2 R7 执行器按需检索

- 方案 A（推荐）：DSH 工具面注册 `weave_knowledge_search`（host-wiring 已有
  `${prefix}knowledge_*` 工具注册模式），执行器会话内可调用；
- 方案 B：provider 级 `mcp_servers` 挂 `dsh-mcp-knowledge`（已有 stdio 服务）；
- 同步移除 prompt 中"P0 无检索 CLI"说明，改为工具用法提示。

### 6.3 R8 图谱注入（P2）

注入段尾部追加"关联知识"摘要：对 Top-K 条目沿 `[[双链]]` 取一跳邻居标题+id，
上限 5 行；不注入全文（防限額膨胀）。

---

## 附录 A · 跨会话教训（本次评审沉淀）

- Git Bash 环境下 `str_replace_editor` 不识别 `/k/...` 挂载路径（解析为 `C:\k\...`）；
  本工程真实路径为 `K:\work\project\weave`——编辑器一律用 `K:/...` 风格路径，
  读取可用 bash `cat -n`/`sed -n`。
- 本仓库 vitest 仅收集 `src/plugins/weave/__tests__/**/*.test.ts`（node 环境）。
