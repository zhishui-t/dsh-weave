# 07 · 任务文档 — Round 4 / P1（增量下发 / 实时回灌 / 画布自适应）

> 依据：`doc/05-反思沉淀与技能落地-优化设计文档.md` §6 P1 正式设计（2026-08-28 架构师落案）。
> T 编号自 **T9** 起续编（T1–T8 已被 Round 3 及并行任务占用）。
> 按文件所有权切分；T9/T11/T13 三条起点任务互不相交可并行；末位 T15 为 QA 全量回归。

---

## T9 · P1-B 节流器纯逻辑（session-stream.ts）

- **subject**: SessionStreamThrottle 滑窗节流/合并/终态 flush 纯逻辑 + 单测
- **建议角色**: developer-1
- **description**:
  - 规格引用：doc/05 §6.2——新建 `src/plugins/weave/session-stream.ts`：
    `StreamOptions`（enabled/minIntervalMs=5000/maxChars=200/events=['output','status']）、
    `SessionStreamThrottle`（纯逻辑，可注入时钟）、`createExecutorEventNotifier` 工厂。
    噪声控制四件套：类型过滤 / 滑窗节流（pending 缓冲）/ 窗口到期合并发送（截断
    maxChars）/ 任务终态（status: completed|error）立即 flush 尾包。整体吞错。
  - 文件所有权：仅新增 `src/plugins/weave/session-stream.ts` +
    `src/plugins/weave/__tests__/session-stream.test.ts`；不碰 delegation-service/index。
  - 验证命令：
    ```bash
    pnpm vitest run src/plugins/weave/__tests__/session-stream.test.ts   # 新增用例全绿（正常/节流窗口/合并截断/终态flush/吞错）
    pnpm typecheck
    ```
- **depends_on**: （无）

## T10 · P1-B 生产接线（delegation context.sessionId + index 装配 + settings 开关）

- **subject**: 执行器事件实时回灌会话的装配接线
- **建议角色**: developer-2
- **description**:
  - 规格引用：doc/05 §6.2——①`SchedulerDelegationContext` 增加 `sessionId` 字段，
    scheduler 调 `executeTask` 时从 run 上下文传入，delegation 发事件时带上
    （`delegation-service.ts`）；②`index.ts` 装配
    `onExecutorEvent: createExecutorEventNotifier({...})`，notify 走既有
    `notifyWeaveSession`；③配置键 `execution_stream` 接入 `loadWeaveSettingsOverrides`
    （settings-store.ts，缺省全默认）。scheduler 仅加一个字段透传，不改泵逻辑。
  - 文件所有权：`delegation-service.ts`（context 类型与事件 sessionId）、
    `scheduler.ts`（context 透传 sessionId 一行）、`index.ts`（装配）、
    `settings-store.ts`（覆盖键）、`__tests__/delegation-service.test.ts`（sessionId 断言）。
    与 T9 的 session-stream.ts 只做 import，不改其内容。
  - 验证命令：
    ```bash
    pnpm vitest run src/plugins/weave/__tests__/delegation-service.test.ts src/plugins/weave/__tests__/scheduler-reflection.test.ts
    pnpm vitest run src/plugins/weave/__tests__/session-stream.test.ts   # T9 产物不回归
    pnpm typecheck
    ```
- **depends_on**: T9

## T11 · P1-C 宿主面板自适应（dag-panel.tsx fitDagLayout）

- **subject**: fitDagLayout 布局函数导出 + dag-panel 容器自适应 + 纯函数单测
- **建议角色**: developer-3
- **description**:
  - 规格引用：doc/05 §6.3——`src/plugins/weave/dag/dag-panel.tsx`：现有 CELL_W 等
    常量改为 base 基准（200/64/48/24），导出 `fitDagLayout(viewport, maxLevel, maxRows,
    base?, floor?)`（floor 56/18/14/4，scale=min(1, vw/w, vh/h)，低于 floor 钳制并
    overflow=true）；面板容器用 ResizeObserver 测量并改用 fit 输出布局，overflow=true
    时保留现滚动行为；字号随 cellH 联动（max(8, 10×cellH/64)px）。既有导出常量名
    保留（供 client 同构对齐），布局调用点切换。
  - 文件所有权：仅 `src/plugins/weave/dag/dag-panel.tsx` +
    `src/plugins/weave/__tests__/dag-panel.test.tsx`。
  - 验证命令：
    ```bash
    pnpm vitest run src/plugins/weave/__tests__/dag-panel.test.tsx   # 既有 6 用例 + 新增 fitDagLayout 纯函数用例全绿
    pnpm typecheck
    ```
- **depends_on**: （无）

## T12 · P1-C client 画布自适应（src/client/index.ts）

- **subject**: 会话面板 DAG 画布参数化 + ResizeObserver 自适应（20/20 保持）
- **建议角色**: developer-1
- **description**:
  - 规格引用：doc/05 §6.3——client 单文件 bundle **禁 import**（ResizeObserver 为
    浏览器全局可直接用）：手工同构移植 fitDagLayout（base 100/32/30/8 与 T11 的
    floor 56/18/14/4 对齐——以 T11 落地的导出签名为准）；DagGraph 用
    useState+ResizeObserver 存 viewport，布局尺寸改用 fit 输出；`.weave-dag-wrap`
    fit 模式 min-height、overflow=true 回落 auto；字号联动 max(8, 10×cellH/32)px。
    **DOM 结构与 data-testid 零变化**，client-bundle 既有 20 用例语义不得改动
    （可选加 1 例小视口收缩断言）。
  - 文件所有权：仅 `src/client/index.ts`（可选 `__tests__/client-bundle.ui.test.tsx`
    追加用例，不改既有断言）。
  - 验证命令：
    ```bash
    pnpm build   # dist 可构建（宿主加载 dist）
    pnpm vitest run src/plugins/weave/__tests__/client-bundle.ui.test.tsx   # 20/20（+可选新增）
    pnpm typecheck
    ```
- **depends_on**: T11（对齐 floor/base 常量与函数签名）

## T13 · P1-A planner 追加语义（append_to + DAG 域编号）

- **subject**: weave_plan_tasks 增量下发：append_to 参数、DAG 域递增 refId、批∪既有依赖校验
- **建议角色**: developer-2
- **description**:
  - 规格引用：doc/05 §6.1——`planner.ts`：①`PlanTasksInput` 增 `append_to?: string`
    （缺省新建，行为不变）；②append 校验：DAG 存在且 team_id 与会话绑定团队一致；
    终态 DAG 允许追加但 dags.status 置回 created；③缺省 refId 改 `T${既有任务数+i}`
    （DAG 域递增，查库计数）；显式 id 查库查重（撞 → invalid_argument）；④
    depends_on 解析域扩为批内新 id ∪ 目标 DAG 既有 refId；⑤批内节点+既有 edges
    联合 assertAcyclic；⑥返回结构含 appended 标记与新增任务列表。**不改 scheduler**
    （重泵走既有 start 幂等，由 T14 验证）。host-wiring/cli-mcp 参数透传一并处理
    （`host-wiring.ts` planTasks 入参类型放宽为 unknown 透传，若已透传则零改动）。
  - 文件所有权：`planner.ts`、`host-wiring.ts`（仅透传需要时）、
    `__tests__/planner.test.ts`（新增 append 用例组：正常追加/编号递增/引用既有依赖/
    显式 id 撞库/DAG 不存在或跨团队拒绝/终态复活/环拒绝/缺省新建回归）。
  - 验证命令：
    ```bash
    pnpm vitest run src/plugins/weave/__tests__/planner.test.ts
    pnpm typecheck
    ```
- **depends_on**: （无）

## T14 · P1-A 调度器联动验证（追加任务入泵 e2e 单测）

- **subject**: append 后 scheduler.start 幂等重泵：新增任务被执行、复活 DAG 收敛
- **建议角色**: developer-3
- **description**:
  - 规格引用：doc/05 §6.1——新建 `__tests__/planner-append.test.ts`（仿
    scheduler-reflection.test.ts 的 FakeDelegation 模式）：①在途 DAG（1 任务
    RUNNING/WAITING）追加 2 任务 → start 重入 → 全部收敛 COMPLETED 且执行顺序
    符合依赖；②completed DAG 追加 → dags.status 回 created → 新任务执行 → DAG
    重新收敛 completed；③追加依赖既有任务：既有完成前新任务 BLOCKED、完成后放行。
    若验证暴露 scheduler 缺口（如 run 上下文重建/通知面），**最小修复 scheduler.ts**
    并在测试中锚定；不改泵语义。planner append 的 schedulerStart 回调接线若在 T13
    未完成（planner handler → scheduler.start 传参），在本任务补齐（动 index.ts
    的 planTasks 装配一处）。
  - 文件所有权：新增 `__tests__/planner-append.test.ts`；必要时 `scheduler.ts`
    （最小修复）与 `index.ts`（装配一处）；不动 planner.ts（问题回报队长转 T13）。
  - 验证命令：
    ```bash
    pnpm vitest run src/plugins/weave/__tests__/planner-append.test.ts
    pnpm vitest run src/plugins/weave/__tests__/scheduler.test.ts src/plugins/weave/__tests__/scheduler-reflection.test.ts   # 调度器回归
    pnpm typecheck
    ```
- **depends_on**: T13

## T15 · QA 全量回归 + P1 一致性检视（收尾闸门）

- **subject**: 四门全绿 + doc/05 §6 逐条核对，追加 QA 报告
- **建议角色**: qa
- **description**:
  - 规格引用：doc/05 §6.1–§6.3 与 doc/07 T9–T14 全部验收项。四门：
    ```bash
    pnpm test && pnpm typecheck && pnpm lint && pnpm build
    ```
  - 一致性核对：①§6.1 append API/编号/入泵语义 ↔ planner.ts+测试；②§6.2 节流
    四件套/开关默认值/sessionId 扩展 ↔ session-stream.ts+装配；③§6.3 fitDagLayout
    数学/floor 常量/双实现对齐 ↔ dag-panel.tsx+client；④client-bundle 20/20 与
    DOM 结构未破坏。小偏差直接修文档登记；实现级问题回报队长重开任务。
  - 产出：`doc/review-report-round4.md`（核对表+四门摘要+遗留）。
  - 文件所有权：仅新增该报告（全绿并获队长授权后可执行收口 commit）。
- **depends_on**: T9, T10, T11, T12, T13, T14

---

## 附：派发速览

| 任务 | subject | 角色 | depends_on | 文件所有权 |
| --- | --- | --- | --- | --- |
| T9 | P1-B 节流器纯逻辑 session-stream.ts | developer-1 | — | `session-stream.ts`（新）+ 单测（新） |
| T10 | P1-B 生产接线（sessionId+装配+开关） | developer-2 | T9 | `delegation-service.ts`、`scheduler.ts`（一行）、`index.ts`、`settings-store.ts`、`delegation-service.test.ts` |
| T11 | P1-C dag-panel fitDagLayout 自适应 | developer-3 | — | `dag/dag-panel.tsx`、`dag-panel.test.tsx` |
| T12 | P1-C client 画布自适应（20/20 保持） | developer-1 | T11 | `src/client/index.ts`（可选 client-bundle.test 追加） |
| T13 | P1-A planner append_to 追加语义 | developer-2 | — | `planner.ts`、`host-wiring.ts`（如需）、`planner.test.ts` |
| T14 | P1-A 调度器入泵联动验证 | developer-3 | T13 | `__tests__/planner-append.test.ts`（新）、`scheduler.ts`（最小修复）、`index.ts`（装配一处） |
| T15 | QA 全量回归+一致性检视 | qa | T9–T14 | 新增 `doc/review-report-round4.md` |

**并行编组建议**：波次一 = T9 ∥ T11 ∥ T13（零交集）；波次二 = T10 ∥ T12 ∥ T14；
波次三 = T15。

---

# P1-D 任务状态变更通知单出口（doc/05 §6.4，2026-08-28 追加）

> 前置：T10/T14/T15 收口后派发（T18/T19 触及 scheduler/cli-mcp，避免与波次一/二并行冲突）。

## T17 · TaskStatusNotifier 纯逻辑

- **subject**: task-status-notifier.ts 单出口通知器 + 单测
- **建议角色**: developer-1
- **description**: 规格引用 doc/05 §6.4——新建 `src/plugins/weave/task-status-notifier.ts`：
  `TaskStatusChange`（含 actor/source）、`TaskStatusNotifier`（`notify` 单条 +
  `notifyBatch` 批量合并 ≤10 行 + `echoSelfActions` 默认 false 回声抑制 + 整体吞错）。
  文案格式按规格；notify 回调注入（测试用收集器）。
  所有权：仅新增 notifier 文件 + `__tests__/task-status-notifier.test.ts`。
- **验证**: `pnpm vitest run src/plugins/weave/__tests__/task-status-notifier.test.ts`；`pnpm typecheck`
- **depends_on**: （无，可与 T15 并行）

## T18 · 调度器侧接线（取消/重试/UI 面板）

- **subject**: onExternalCancel/onExternalRetry/repository.cancelTask 发电
- **建议角色**: developer-2
- **description**: 规格接线点 1/2/4——`scheduler.ts`：onExternalCancel（→CANCELLED，
  actor=user）、onExternalRetry（:173 恢复批量，notifyBatch）；`dag/repository.ts`：
  cancelTask（:178 CANCELLED + :188 传播批量）。notifier 经构造 options 注入（缺省
  不发，保持向后兼容）。同步补齐对应 `task.status_changed` 审计。所有权：
  `scheduler.ts`、`dag/repository.ts`、`__tests__/scheduler.test.ts`、
  `__tests__/dag-panel.test.tsx`（如 cancelTask 断言需同步）。
- **验证**: `pnpm vitest run src/plugins/weave/__tests__/scheduler.test.ts src/plugins/weave/__tests__/dag-panel.test.tsx`；`pnpm typecheck`
- **depends_on**: T15, T17

## T19 · 治理与反馈侧接线（CLI/MCP/反馈路由/恢复）

- **subject**: cli-mcp 治理、feedback-router、recovery 发电 + 审计补齐
- **建议角色**: developer-3
- **description**: 规格接线点 3/5/6——`cli-mcp.ts:207/:250`（task_skip/task_patch）、
  `feedback-router.ts`（feedback_* 五动作，含 closeExpired 批量）、`recovery.ts`
  task_repaired（与既有 audit.record 同位置）。actor 标注：CLI/MCP=captain（回声抑制
  下默认不发）、feedback=user、recovery=recovery。所有权：`cli-mcp.ts`、
  `feedback-router.ts`、`recovery.ts`、`__tests__/cli-mcp.test.ts`、
  `__tests__/feedback-router.test.ts`、`__tests__/recovery.test.ts`。
- **验证**: `pnpm vitest run src/plugins/weave/__tests__/cli-mcp.test.ts src/plugins/weave/__tests__/feedback-router.test.ts src/plugins/weave/__tests__/recovery.test.ts`；`pnpm typecheck`
- **depends_on**: T17（T18 无文件交集，可并行）

## T20 · QA 回归 + P1-D 核对清单

- **subject**: 四门 + §6.4 六组接线点逐条核对
- **建议角色**: qa
- **description**: 四门（test/typecheck/lint/build）+ 按 §6.4 接线点清单逐条核对
  （通知文案/actor 标注/批量合并/回声抑制/审计补齐），产出报告追加
  `doc/review-report-round4.md`（或独立 round5）。所有权：仅报告文件。
- **验证**: `pnpm test && pnpm typecheck && pnpm lint && pnpm build`
- **depends_on**: T17, T18, T19
